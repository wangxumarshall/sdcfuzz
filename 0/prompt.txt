/loop 7个小时后执行如下任务：根据 @docs目录中的要求，完成后续所有工作：16 项分短期/中期/长期/工程化，覆盖扩大注入规模、多 bit 翻转、扩展 MUT 槽、长时扫描、0201 接入、EDA耦合、老化测试、Vmin 扫描、多核一致性、演化实战、脆弱性测绘、业务画像、学术发表等。必须诚实、不能说谎、必须100%服从事实、所有工作和结果必须基于事实并且经过严格的逻辑推理或实证，永远尊重事实、永远真诚。

---

7小时定时已到。现在执行后续工作（必须诚实、不能说谎、100%服从事实、所有结果基于实证或严格逻辑推理）。

根据 docs/plan/kunpeng920_sdc_plan.md 附录C.2 + docs/kunpeng920_sdc_research_report.md 第7章下一步工作的16项，按优先级实际执行。对可立即开展的项必须真跑命令拿真实输出；对需外部条件/长期调研的项（如 EDA Gate清单获取、老化烤机箱、Vmin调压、学术发表）必须如实记录"为何不能在本次完成"而非谎称完成。

具体执行顺序：
1. 先读 docs/kunpeng920_sdc_research_report.md 第7章确认16项清单与现状，git log 确认当前进度。
2. 短期可立即开展项（真跑）：
   - 扩大 gem5-fi 注入规模到 500+ 次，统计 diverge 分布
   - 多 bit 翻转注入（max-faults > 1）对比单 bit diverge 率
   - 扩展剩余 13 模板的 MUT 槽，变体数从 65 扩到 200+
   - 0201 单板（172.168.178.81）可达性重测，恢复则接入分布式集群
   - 演化闭环 sdc_evolve.sh 在检出 SDC 时实战触发
3. 需外部条件/长期项（如实记录不能完成的真实原因）：
   - 长时24h真机扫描（可启动但要24h，本次仅启动并记录）
   - EDA Gate-level 覆盖率耦合（需芯片设计端 Gate 清单，不可得）
   - 老化加速测试（需85°C烤机箱，无设备）
   - Vmin电压裕量扫描（需DVFS调压权限/硬件支持）
   - 多核一致性LSE专项（可基于现有模板扩展）
   - 微架构脆弱性测绘/业务画像/学术发表（长期调研）
4. 每完成一项立即 git commit + push（one-patch-per-unit），commit message 引用真实命令输出。
5. 全部完成后更新 docs/kunpeng920_sdc_research_report.md 第7章，把"下一步"改为"已完成/进行中/待外部条件"三档，附实证数据。